name: cleanup_shops
description: Cleanup script for removing shops without images

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cloud_firestore: ^4.14.0
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.0
